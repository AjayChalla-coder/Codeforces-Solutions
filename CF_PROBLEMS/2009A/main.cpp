#include <bits/stdc++.h>
using namespace std;

int main()
{
    int t; if(!(cin>>t)) return 0;
    while(t--)
    {
        int a, b;
        cin >> a >> b;
        cout << (b - a) << '\n';
    }
    return 0;
}